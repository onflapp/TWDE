#!/bin/bash

CFG="$TWDE_HOME/var/widget.xrm"
prepareconf "$TWDE_HOME/lib/widget.xrm" \
  $TWDE_HOME/etc/theme.conf > "$CFG"

xrdb -merge "$CFG"

exec xterm -name TWDEWidget -class TWDEWidget -e "$@" &
