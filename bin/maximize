#!/bin/bash

#SIZE=`property get ORIGINAL_SIZE`

source <(xdotool getwindowfocus getwindowgeometry --shell)
echo "$WIDTH"

#property set "ORIGINAL_SIZE" "$X $Y $WIDTH $HEIGHT"

#xdotool windowsize $WINDOW $WIDTH 100%
#xdotool windowmove $WINDOW $X 0
